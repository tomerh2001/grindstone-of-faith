{
  "name": "grindstone-of-faith",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check site/app.js && node --check site/model.js && node --check site/simulation.js && node --check site/simulation-view.js"
  },
  "devDependencies": {
    "jsdom": "26.1.0"
  }
}
